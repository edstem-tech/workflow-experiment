name: Continuous Deployment
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.ref }}
    steps:
      - name: my-step
        env:
          runner: ${{ secrets.runner }}
        shell: bash
        run: echo $runner